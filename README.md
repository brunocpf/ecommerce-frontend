# [WIP] ecommerce-frontend

A fully responsive ecommerce PWA template, built with Next.js (React), TailwindCSS, GraphQL, Stripe and framer-motion.

To run locally:

```bash
npm run dev

# or

yarn dev
```

And open http://localhost:3000
